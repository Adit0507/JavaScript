let x = 1 + 2 + " House of Balloons";
let y = "What u need " + 20 + 10;  // "What u need" + "20"  // "What u need" + "10"

let a = "34";
let b = "5";
let c = "5";

let rem = a%b;
let sub = b - c;
let mul = a*b;

console.log(x,y);
console.log(rem , isNaN(sub), mul);  // isNan  NaN denotes 'Not a Number' and this represents that the output is not a valid number. 
                                    // You can use isNaN() function to check if the given expression evaluates to a valid number or not. 
                                   //If this function returns true, then the expression or literal is not a valid number. 
                                  //If the result is false, the expression or literal is a valid number.

